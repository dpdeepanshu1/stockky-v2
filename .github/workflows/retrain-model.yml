name: Retrain Prediction Model

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  retrain:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: services/decision-prediction-service/prediction
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Wake up backend services (optional)
        env:
          API_GATEWAY_URL: ${{ secrets.API_GATEWAY_URL }}
          MARKET_DATA_URL: ${{ secrets.MARKET_DATA_URL }}
        run: |
          echo "Waking up services (if secrets are set)..."
          if [ -n "$API_GATEWAY_URL" ]; then
            curl -s -o /dev/null -w "%{http_code}" "$API_GATEWAY_URL/health" || true
          else
            echo "API_GATEWAY_URL secret not set – skipping wake-up"
          fi
          if [ -n "$MARKET_DATA_URL" ]; then
            curl -s -o /dev/null -w "%{http_code}" "$MARKET_DATA_URL/health" || true
          fi
          sleep 10

      - name: Retrain model
        working-directory: services/decision-prediction-service/prediction
        run: |
          # Try normal name first, then possible renamed version
          if [ -f train.py ]; then
            python train.py
          elif [ -f pred_train.py ]; then
            python pred_train.py
          else
            echo "ERROR: Neither train.py nor pred_train.py found"
            ls -la
            exit 1
          fi

          mkdir -p ../../../data/prediction-model
          cp model.pkl ../../../data/prediction-model/model.pkl || cp *.pkl ../../../data/prediction-model/ || true

      - name: Commit and push updated model
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/prediction-model/ || true
          if git diff --staged --quiet; then
            echo "No changes to model"
          else
            git commit -m "Auto-retrain prediction model [skip ci]"
            git push
          fi