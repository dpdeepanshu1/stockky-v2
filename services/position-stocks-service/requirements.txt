fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
httpx>=0.27.0
pyotp>=2.9.0
websockets>=12.0
cryptography>=42.0.0
oracledb==2.5.1
dhanhq>=2.0.2
# pytz removed (AUDIT FIX): tz_utils.py uses stdlib zoneinfo, not pytz — pytz was never imported anywhere in this service.
# tzdata provides timezone data on systems where /usr/share/zoneinfo may be missing (slim Docker base images).
tzdata
# Admin auth (auth/admin_auth.py) — SAME libs/versions real-trade-service
# pins for the identical Argon2id-hash + signed-JWT mechanism.
argon2-cffi==23.1.0
pyjwt==2.8.0
